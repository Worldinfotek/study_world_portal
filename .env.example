# Copy to .env on the Plesk server, or set these in Plesk > Node.js > Custom environment variables.
# Do not upload .env.local.

NODE_ENV=production
PORT=3000

# Linux Plesk cannot use .\MSSQLSERVER2022. Use the SQL Server IP and port 1433.
SQL_HOST=74.50.79.178
SQL_DB_NAME=study_world_portal
SQL_AUTH=sql
SQL_USER=portal_admin
SQL_PASSWORD=

GEMINI_API_KEY=
